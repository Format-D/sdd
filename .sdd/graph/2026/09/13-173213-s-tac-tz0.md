---
type: signal
layer: tactical
kind: done
closes:
    - 20260913-141434-d-tac-46x
participants:
    - Christopher
confidence: high
topics:
    - implementation/mcp
    - portability/mcp
    - portability/runtime
    - reliability/testing
summary: Merges stateless Streamable HTTP on `main` (commit `19acbbfb`) with per-request caller authorization, workflow-state reconstruction from the durable session log, dedup-preserving replay, and on-demand attachment materialization under the existing source authority and write API. This completes the tactical directive committing to Streamable HTTP serving with unchanged stdio (20260913-141434-d-tac-46x) with no recorded deviation.
---

Merge commit `19acbbfb` on `main` ships stateless Streamable HTTP with local stdio unchanged, removes the transitional transport option, and makes each attached-session request authorize its caller and reconstruct workflow state from the durable session log. Replay now preserves served-content deduplication, explicit resume reorients the session, and stale append conflicts stop with resume guidance rather than retrying stale events. The shared snapshot inventory enumerates graph documents and attachment names without opening attachment bodies, while materialization reads documents and acquired attachment readers fetch content on demand under the existing source authority and write API. This completes 20260913-141434-d-tac-46x with no recorded deviation.
