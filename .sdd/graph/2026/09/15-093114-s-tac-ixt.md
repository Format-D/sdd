---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260914-113911-d-tac-wgw
      kind: grounded-in
      desc: withdraws the digest and size checks still enforced on the prepared-write path
    - id: 20260915-085829-s-tac-awi
      kind: surfaced-by
      desc: the review of this delivery produced the findings
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - reliability/testing
    - evaluation/inner/code-review
summary: 'Code review of the capture-publication delivery (20260915-085829-s-tac-awi) recorded unfixed findings at merge `547ef763`: layering violations, event-position prediction from the binding version, MCP field tags on application structs, tool-call syntax in engine error text, test-placement smells, and digest/size checks still enforced on the prepared-write path. It is grounded in the directive (20260914-113911-d-tac-wgw), whose withdrawals the code fails to honor. None of the findings blocked the merge; each is a small cut for a later session.'
---

Code review of the capture-publication delivery recorded in 20260915-085829-s-tac-awi left findings unfixed at merge `547ef763`: application code decodes raw engine events and keeps a second staged-filename projection, the event position is predicted from the binding version instead of read from the store, application structs carry MCP field tags, engine error text carries tool-call syntax, digest and size checks withdrawn by 20260914-113911-d-tac-wgw stay enforced on the prepared-write path, and test smells span the engine, application and MCP packages. Each finding with its location is in [review findings](./15-093114-s-tac-ixt/review-findings.md). Expected: the layering, fail-loud, comment and test-placement conventions in `AGENTS.md`, and the withdrawals in 20260914-113911-d-tac-wgw. None blocked the merge; each is a small cut a later session can take from the list.
