---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260914-001550-d-tac-n47
      kind: grounded-in
      desc: the expectation that creation never allocates another entry for the same intent
    - id: 20260915-085829-s-tac-awi
      kind: surfaced-by
      desc: found while reviewing this delivery
participants:
    - Christopher
confidence: medium
topics:
    - implementation/engine
    - reliability/discipline
summary: 'Tactical gap signal: the engine''s retry of a recorded intent skips the transition owed by the dispatching chooser option, so a mutation dispatched from an option''s `call` is re-served at the same chooser and a second answer prepares a second intent that the publication key cannot deduplicate. Found in `Session.Retry` at merge `547ef763` while reviewing the capture delivery (s-tac-awi); it violates the grounded expectation that creation never allocates another entry for the same intent (d-tac-n47). Latent today — no shipped procedure dispatches a mutation from an option — and confidence is medium since no test reproduces it; candidate remedies (replay the option''s transition or refuse such retries) are unsettled.'
---

The engine's retry of a recorded intent skips the transition the dispatching chooser option owed. Found while reviewing the delivery recorded in 20260915-085829-s-tac-awi, by reading `Session.Retry` at merge `547ef763`: it re-runs the command, marks the current step's op done and cascades, but a mutation dispatched from an option's `call` is followed by that option's transition only on the original path, so the instance is re-served at the same chooser and a second answer prepares a second intent, which the publication key cannot deduplicate. Expected: creation never allocates another entry for the same intent, per 20260914-001550-d-tac-n47. Latent today, since no shipped procedure dispatches a mutation from an option, and not reproduced by a test, which is why confidence is medium. Candidate remedy, unsettled: replay the dispatching option's transition on retry, or refuse a retry whose intent step is not the current step's op.
