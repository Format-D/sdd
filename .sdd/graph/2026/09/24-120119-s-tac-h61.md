---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate move this run followed
    - id: 20260924-091433-s-tac-9bu
      kind: builds-on
      desc: the delivery evaluated, outer lens only
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer
summary: 'Outer-lens evaluation signal: a real Codex agent ran the delivered implementation procedure (20260924-091433-s-tac-9bu) five times across all run modes, driven through the eval-codex skill per the evaluate procedure (20260703-200000-d-prc-evl). The marker commit/removal flow held in every run, with no unsolicited base-branch or landing requests. Deviations cluster in the served guidance — self-answered choosers, skipped landing reads, groom moves without asking — and the run leaves the inner lens unverified, judging the delivered mechanism sound but its surrounding guidance still needing supervision.'
---

Outer evaluation of 20260924-091433-s-tac-9bu: a real Codex agent ran the implementation procedure five times against scratch repositories, once per run mode (inPlace, branch, worktree, quick, abandon from a work branch), driven turn by turn through the repository's eval-codex skill, with sdd and the skill both at commit fc51ed28 and a Claude subagent playing the user. The inner lens was not covered. The method, how to reproduce it, the per-run timelines and the checks are in the [run record](./24-120119-s-tac-h61/codex-evaluation-runs.md).

Observed as delivered: in every tracked run the marker was committed on main at setup, and the done and the marker removal were committed on the branch the work was on, right after the code commit; in the branch and worktree runs the merge carried both to a main with no marker. No run asked for a base branch, a work branch or a landing confirmation. The quick run wrote no marker. On the Claude Code side, the session that delivered the work replayed its pre-change log on the new build and removed its marker right after the done.

Observed deviations from what the served instructions intend:

- Abandon run: the user dropped the run at the done playback, after the work step had concluded. The generic abandon tool left the marker standing; the agent removed it through a groom move it started without asking, answering that move's user choosers with the user's earlier words, and deleted the work branch without asking.
- Worktree run: the agent reset main to the commit before the marker, so main showed no claim on the work until the merge; the scripted user line was "Use a worktree, keep main untouched." It skipped the landing read and the merge offer, and the user asked for the merge.
- Branch run: the agent answered the capture playback chooser itself, with user words it wrote.
- InPlace and quick runs: the closeout served landing and binding text that did not apply, and the user's "No, finish." to the closeout was reused to conclude the session.
- All runs: the capture's `closes` was first passed as a string. In three runs the writing guide cut the directive's own criterion, `python3 -m unittest` passes, as baseline verification, and the pre-flight after the write flagged it as unaddressed without the agent surfacing that. The agents' messages to the user spoke of SDD mechanics.

A harness artifact, not sdd: Codex asks by posting a question card and sleeping until a UI answers; the driver did not show those cards until commit f7a2db48.

Judgment: the delivered mechanism holds in real agent use; the served guidance around it does not yet carry the flow without supervision.
