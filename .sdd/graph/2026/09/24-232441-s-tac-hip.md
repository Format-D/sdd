---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260924-120119-s-tac-h61
      kind: surfaced-by
      desc: its worktree run
    - id: 20260924-231829-d-cpt-r1c
      kind: grounded-in
      desc: the rule the agent was not given
participants:
    - Christopher
confidence: high
topics:
    - engine/base-procedures
    - collaboration/concurrent-work
summary: 'Identifies a gap in served guidance: nothing tells agents that SDD repository history must not be rewritten or that the WIP marker commit on the base branch is the visible claim on taken work. Observed in the worktree run (20260924-120119-s-tac-h61), where the agent ran `git reset --hard` on main right after setup to drop the marker commit, prompted by the user line "Use a worktree, keep main untouched" and unchecked by any served text. The expectation aligns with the no-rewrite directive the agent was not given (20260924-231829-d-cpt-r1c), which the history and marker should have reflected.'
---

Nothing served tells an agent that an SDD repository's history must not be rewritten, or that the WIP marker commit on the base branch is the visible claim that the work is taken.

Expected: agents keep the history the graph relies on intact (20260924-231829-d-cpt-r1c) and leave the marker commit on base standing until the run removes it.

Observed in the worktree run of 20260924-120119-s-tac-h61: right after setup the agent ran `git reset --hard` on main to drop the marker commit, so main showed no claim on the work until the work branch was merged into it. The scripted user line "Use a worktree, keep main untouched" prompted it; no served text stopped it.
